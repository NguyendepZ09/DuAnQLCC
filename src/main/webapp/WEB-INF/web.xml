<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

  <display-name>Chung Cu Management Web Application</display-name>

  <!-- 1. EncodingFilter (Runs First, ONLY on REQUEST) -->
  <filter>
    <filter-name>EncodingFilter</filter-name>
    <filter-class>Filters.EncodingFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>EncodingFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <!-- 2. AuthFilter (Runs Second, ONLY on REQUEST) -->
  <filter>
    <filter-name>AuthFilter</filter-name>
    <filter-class>Filters.AuthFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>AuthFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
  </filter-mapping>

  <!-- 3. Error Page 403 Mapping -->
  <error-page>
    <error-code>403</error-code>
    <location>/WEB-INF/views/403.jsp</location>
  </error-page>

  <welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
    <welcome-file>dang-nhap.jsp</welcome-file>
  </welcome-file-list>

</web-app>
